# Seperating modules into different files

**`mod`** is unlike the **`include`** statement in other languages. Once a module has been declared in the entire module tree, it does not need to be declared again in any other file. It will be accessed using a path relative to where it was declared.

For a submodule defined in another file, the file should be placed in a folder of the name of its ancestor.

---
